{
    "description": "Dev-only tooling. The application itself has zero dependencies (ADR-001); this exists solely so CI can produce code coverage via php-code-coverage.",
    "require": {
        "php": ">=8.2"},
    "require-dev": {
        "phpunit/php-code-coverage": "^11"
    }
}
