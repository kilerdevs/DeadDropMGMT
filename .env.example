# Compose overrides — copy to .env and adjust. Everything has a working default.
#APP_PORT=8080
#DB_PASS=deaddrop-db
#DB_ROOT_PASS=deaddrop-root
# 64-char hex AES key; leave unset to auto-generate + persist on the config volume
#DDMGMT_AES_KEY_HEX=
